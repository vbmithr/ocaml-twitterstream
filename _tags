true: debug, bin_annot

<src/twitterstream.*>: package(lwt), \
                       package(lwt.ppx), \
                       package(hex), \
                       package(nocrypto.lwt), \
                       package(cohttp.lwt), \
                       package(re.str), \
                       package(yojson)
